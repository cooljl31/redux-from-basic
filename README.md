# This is just a basic react app with redux-form

* if you clone this project remember to install json-server

```bash
yarn global add json-server &&  yarn install && yarn start
```

after you're gooood to go :)